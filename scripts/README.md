# Scripts Directory

This directory contains utility scripts for the LogiVox project.

## Moved Scripts

- `generate-brand-assets.js` - Script for generating brand assets.
- `generate-high-res-logos.js` - Script for generating high-resolution logos.

## Start-up Validator

The `startup-validator.js` script (formerly `server.js`) has been moved to `docs/archive/legacy-scripts/` as it referenced non-existent files and appeared to be a misplaced artifact.
